const numbers = require('./numbers');
const suffixes = require('./suffixes');
const highways = require('./highways');
const directions = require('./directions');
const abbreviations = require('./abbreviations');
const inverseNumbers = require('./inverse/numbers');
const inverseSuffixes = require('./inverse/suffixes');
const inverseHighways = require('./inverse/highways');
const inverseDirections = require('./inverse/directions');
const inverseAbbreviations = require('./inverse/abbreviations');

module.exports = {
    numbers,
    suffixes,
    highways,
    directions,
    abbreviations,
    inverseNumbers,
    inverseSuffixes,
    inverseHighways,
    inverseDirections,
    inverseAbbreviations,
};
